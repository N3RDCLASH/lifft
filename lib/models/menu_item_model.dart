import 'package:flutter/material.dart';

class MenuItem {
  final String name;
  final Icon icon;
  final Widget page;
  final AppBar appBar;

  MenuItem({this.name, this.icon, this.page, this.appBar});
}
