import 'package:flutter/material.dart';

class CreateWorkout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create workout'),
      ),
      body: Text("create workout screen"),
    );
  }
}
